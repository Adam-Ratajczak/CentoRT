schemaVersion: 1

workspace:
  projects:
    - name: math_rs
      dependsOn: []
      targets:
        - name: math
          type: dylib
          language: rs
          toolchain: cargo
          sources: ["src/**/*.rs"]
          link: []
          intDir: "${BUILD_DIR}/${CENTO_CURRENT_PROFILE}/obj/math"
          outDir: "${BUILD_DIR}/${CENTO_CURRENT_PROFILE}/app"
          bridges:
            implements: ["{$CENTO_WORKSPACE_DIR}/idl/math/**/*.idl"]
            uses: []